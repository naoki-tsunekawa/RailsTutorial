<%# provideで"layouts/application.html.erb"のtitleタグに"All users"を表示 %>
<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= paginate @users %>

<ul class="users">
  <%= render @users %>
</ul>

<%= paginate @users %>
